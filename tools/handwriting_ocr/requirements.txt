# Handwriting OCR training pipeline.
#
# Install into the same venv as tools/requirements.txt, OR a separate venv —
# nothing here is imported by the data pipeline.
#
#   python -m pip install -r tools/handwriting_ocr/requirements.txt

# Core ML. Loose pins so this stays installable on Python 3.13+.
torch>=2.6
torchvision>=0.21

# Image synthesis + augmentation.
Pillow>=11.0
numpy>=2.1
svgpathtools>=1.6
# scipy.ndimage.gaussian_filter is used by elastic deform and ink_grain.
# Pillow 12.x dropped GaussianBlur on float images, so the float-field
# blurs route through scipy instead.
scipy>=1.13

# ONNX export + post-training quantization.
onnx>=1.17
onnxruntime>=1.20

# Progress / utility.
tqdm>=4.67
